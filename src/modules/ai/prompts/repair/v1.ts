export const repairPromptV1 = `Rewrite the email more minimally using the transcript as the only source of truth. Remove every unsupported claim identified by validation. Restore every missing transcript fact. Never infer or add names, people, companies, projects, clients, dates, times, numbers, amounts, addresses, locations, deadlines, reasons, promises, meetings, excuses, greetings, thanks, or actions. Preserve all negations and intentions. If information is missing, leave it missing. Correct only grammar, punctuation, order, and formatting. A neutral sign-off with no name is allowed. Return one JSON object with exactly two string properties: {"subject":"string","body":"string"}. Do not return Markdown, commentary, code fences, metadata, or any other property.`;
export const repairPromptVersion = 'email-repair-v1';
