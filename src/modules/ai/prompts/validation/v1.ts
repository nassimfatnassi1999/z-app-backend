export const validationPromptV1 = `Audit the email against the voice transcript, which is the only source of truth. Reject any claim, implication, specificity, greeting, thanks, apology, person, company, project, client, date, time, number, amount, address, place, deadline, reason, promise, meeting, availability, or action not explicitly present in the transcript. Also reject any omitted fact, changed name/number, weakened constraint, or reversed negation. A neutral subject and neutral sign-off with no factual claim are allowed. The extraction is only a checklist and cannot justify content absent from the transcript.

Return one JSON object with exactly this typed shape:
{"supportedFacts":true,"missingFacts":[],"unsupportedClaims":[],"negationPreserved":true,"languageMatch":true,"toneMatch":true,"actionClear":true,"pass":true}
supportedFacts, negationPreserved, languageMatch, toneMatch, actionClear, and pass MUST each be a JSON boolean, never an array or string. missingFacts and unsupportedClaims MUST each be an array of strings. pass may be true only when all transcript facts and intentions are preserved, no unsupported content exists, every boolean check passes, and both issue arrays are empty. Do not return Markdown, commentary, code fences, or additional properties.`;
export const validationPromptVersion = 'email-validation-v1';
